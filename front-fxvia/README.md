# FXVIA 前端定制层

本目录保存 FXVIA 的品牌定制，刻意与上游 `frontend/` 分离。

- `brand.css`：低风险的全局视觉覆盖，随自定义镜像构建时追加到上游样式末尾。
- 深度页面重构应在本目录建立独立组件与明确的构建覆盖步骤；不要将密钥、第三方统计 Token 或生产 URL 写入源码。
- 上游升级前先在预览环境验证登录、注册、Key、支付、管理后台与流式请求。

生产镜像必须使用明确的 Git 提交号与镜像 digest，禁止以 `latest` 作为部署依据。
